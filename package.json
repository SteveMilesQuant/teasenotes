{
  "name": "teasenotes",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix onboarding run dev",
    "build": "npm --prefix onboarding run build",
    "preview": "npm --prefix onboarding run preview",
    "install:all": "npm --prefix onboarding install"
  }
}
